{
  "name": "week-18",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "postinstall": "turbo run prisma:generate",
    "prisma:generate": "prisma generate",
    "vercel-build": "prisma migrate deploy && turbo run build"


    
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/bcryptjs": "^2.4.6",
    "eslint": "^8.0.0",
    "eslint-config-turbo": "latest",
    "prettier": "^3.2.5",
    "turbo": "latest",
    "@prisma/client": "^5.21.1",
    "prisma": "^5.21.1"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "framer-motion": "^11.12.0",
    "react-icons": "^5.3.0",
    "zod": "^3.23.8"
  }
}
