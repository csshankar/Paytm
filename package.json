{
  "name": "week-18",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "postinstall": "cd packages/db && npx prisma generate && npx prisma migrate deploy && npx prisma db seed"
    
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/bcryptjs": "^2.4.6",
    "eslint": "^8.0.0",
    "eslint-config-turbo": "latest",
    "prettier": "^3.2.5",
    "turbo": "latest",
    "@prisma/client": "^5.21.1",
    "prisma": "^5.21.1"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "framer-motion": "^11.12.0",
    "react-icons": "^5.3.0",
    "zod": "^3.23.8"
  }
}
